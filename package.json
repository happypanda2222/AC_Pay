{
  "name": "ac-pay",
  "version": "1.0.0",
  "private": true,
  "description": "Static AC Pay calculator PWA.",
  "scripts": {
    "test": "echo \"No automated tests; static site.\" && exit 0",
    "lint": "echo \"No automated linters configured.\" && exit 0"
  }
}
